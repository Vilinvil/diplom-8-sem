function [points] = convert_lines_in_points(lines)
for i = 0:length(lines)
    points = [points; lines(i).point1]
   xy = [; lines(i).point2];
   plot(xy(:,1),xy(:,2),'LineWidth',2,'Color','green');

   len = norm(lines(k).point1 - lines(k).point2);
   if ( len > maxLen)
      maxLen = len;
      xyLong = xy;
   end
end
end

